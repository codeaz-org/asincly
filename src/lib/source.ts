// AGPL-3.0 §13: people using a (possibly modified) deployment over the
// network must be offered its source. Self-hosters who change the code
// should point this at their own fork.
export const SOURCE_URL = process.env.NEXT_PUBLIC_SOURCE_URL || "https://github.com/codeaz-org/asincly";
